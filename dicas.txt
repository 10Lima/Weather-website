venv\Scripts\activate (rodar maquina virtual)
  python manage.py runserver   ( rodar o servidor)
    
atualizar ficheiros no reposito(escrever estes comandos na bash do git)
    git add .
    git commit -m "O que fizeste ex:(atualização no codigo)"
    git push
    
python -m venv venv (maquina virtual)

pip install -r requirements.txt (instalar)

redis-server

